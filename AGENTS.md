# Agent Instructions

**CRITICAL:** All commands, requests, and instructions sent to agents must be prefixed with "caveman". This applies to every interaction without exception.

Example:
- Instead of: "Create a new component"
- Use: "caveman Create a new component"

This prefix signals the agent to use ultra-compressed communication mode (caveman) for token efficiency.